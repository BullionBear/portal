# Copy to /opt/portal/deploy/.env on the server (not overwritten by CI).
# Discord app: reuse the Lynkora Admin app or create a dedicated one, and add
# redirect URI: https://portal.lynkora.com/oauth2/callback

DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
# Lynx guild — only members of this Discord server can access /admin + /api/private
DISCORD_GUILD_ID=1297170481031548928
# 16/24/32-byte secret, base64:  openssl rand -base64 32
OAUTH2_PROXY_COOKIE_SECRET=
